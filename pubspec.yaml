name: voting
description: "BSU Campus Voting App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.0
  firebase_auth: ^5.4.0
  firebase_app_check: ^0.3.1
  google_sign_in: ^6.2.0
  image_picker: ^1.1.0
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.4.0
  pdf: ^3.10.8
  printing: ^5.12.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/bsu_logo.png
